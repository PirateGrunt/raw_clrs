---
title: 'RMarkdown'
author: "Emma Li"
output:
  slidy_presentation:
    duration: 45
    fig_height: 3
    fig_width: 6
  beamer_presentation:
    colortheme: dolphin
---

## Create a new project
1. Data Cleaning and Summary
2. Data Transformation and Visualization
3. Chain Ladder
4. Additive Model
5. Model Comparison and Selection

## Implementation
* Option A
    + Calculate new R objects, such as statistics, new variables, tables, plots and etc. in RStudio
    + Copy and paste them into PowerPoint. 
    + Pro: easy to make adjustment of the formats
    + Con: time consuming when coding and formating in 2 different files
    
* Option B
    + Create a RMarkdown file including R source codes and markdown syntaxes (a plain text format)
    + Output to PDF, Word or HTML
    + Pro: code and format in 1 file
    + Con: have to learn a new R package
    
R                               | R Markdown
--------------------------------|-------------------------------------------
Run in R base system or RStudio |Run in RStudio 
R. file                         |rmd. file       
R codes without markdown syntax |R codes written in chunks or inline   
"Run" (Control+R for Shortcut)  |"Run" or "Knit HTML" or "Knit PDF" or "Knit Word"


## Introduction of RMarkdown
* RMarkdown is a format that combines R source codes with markdown syntaxes

* We can create rmd. file in RStudio 

* In a rmd. file, there are 3 components

    + 1. YAML Header: what type of document to build 
    
    ![](0. YAML Header.png)

    + 2. R Chunk: tables, plots, regressions by R source codes
    
    ![](0. R Chunk.png)
    
    + 3. Markdown Symtax: headers, equations, images, lists, tables
    
    ![](0. Markdown Symtax.png)


## Installation and Basic
1. Open an R. file and install rmarkdown packages from CRAN 

![](1. Install rmarkdown.png)

## Installation and Basic 
2. Open an rmd. file which creats HTML output

![](2. Open R Markdown.png)

![](2. Chose HTML.png)

## Installation and Basic 
3. In the template rmd. file, run R codes, Knit HTML, Knit PDF or Knit Word

![](3. Run.png)

## 1. YAML Header

Open 60_RMarkdown Example Unordered.rmd

![](4. 1. YAML.png)

* At the top of the rmd. file

* Embeded with lines of 3 dashes

* Change title, name and date

* Change output from "html_document" to "word_document" or "pdf_document""

* YAML options, http://rmarkdown.rstudio.com/pdf_document_format.html

## 2. R Chunk

Kit PDF by Review 60_RMarkdown Example Unordered.rmd

* Embeded with lines of 3 left quotes

![](4. 2. R Chunk.png)

* Chunk options, http://yihui.name/knitr/options/

    + warning=FALSE does not print warnings in the output document (warning=TRUE is default)

    + message=FALSE does not print message in the output document (similar to warning, error=TRUE is default)

    + echo=FASLE turns off the R source code in the output file (echo=TRUE is default)

* Besides, inline codes outside of the R chunks can still print results

    + ![](4. 2. R Chunk inline.png) can print ![](4. 2. R Chunk inline 2.png)

## 3. Markdown Syntax

Kit PDF by  60_RMarkdown Example Unordered.rmd

* Header

![](4. 3. Markdown Syntax header.png)

* Equation

![](4. 3. Markdown Syntax equation.png)

* Image

![](4. 3. Markdown Syntax images.png)

* Unordered List

![](4. 3. Markdown Syntax unorder list.png)

* Ordered List 

![](4. 3. Markdown Syntax order list.png)

* Table

![](4. 3. Markdown Syntax tables.png)

## Reorder the R source codes and Markdown syntaxes and add more relavant ones

Open 60_RMarkdown Example Ordered.rmd. It contains the new project. 

1. Data Cleaning and Summary
2. Data Transformation and Visualization
3. Chain Ladder
4. Additive Model
5. Model Comparison and Selection

## Create outputs of the new project 

Kit 60_RMarkdown Example Ordered.pdf

Kit 60_RMarkdown Example Ordered.word

Kit 60_RMarkdown Example Ordered.html

## Q & A
